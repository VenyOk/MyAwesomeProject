# --- Core web server ---
fastapi>=0.110
uvicorn[standard]>=0.29

# --- Local LLM (Gemma 4 12B) ---
# NOTE: PyTorch must be installed separately with a CUDA build that matches your GPU.
# RTX 5070 Ti (Blackwell, sm_120) needs CUDA 12.8, e.g.:
#   pip install torch==2.7.1+cu128 --index-url https://download.pytorch.org/whl/cu128
# Gemma 4 (model_type: gemma4) requires transformers >= 5.5; use >= 5.12 to also
# get the multimodal+4bit dtype fix (transformers issue #46899, fixed in #46904).
transformers>=5.12
accelerate>=0.34
bitsandbytes>=0.49.2       # native Windows wheel includes sm_120 kernels with cu128 torch

# --- Memory: embeddings + vector index ---
sentence-transformers>=3.0
faiss-cpu>=1.8
numpy>=1.26

# --- Dev / test ---
pytest>=8.0
httpx>=0.27
